/**
 * @copyright   2017, Miles Johnson
 * @license     https://opensource.org/licenses/MIT
 * 
 */

import { SEQUENCE_REMOVAL_PATTERN } from './constants';

export default function fromUnicodeToHexcode(unicode) {
  var strip = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;

  var hexcode = [];

  var _iteratorNormalCompletion = true;
  var _didIteratorError = false;
  var _iteratorError = undefined;

  try {
    for (var _iterator = unicode[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
      var codepoint = _step.value;

      var hex = codepoint.codePointAt(0).toString(16).toUpperCase();

      while (hex.length < 4) {
        hex = '0' + hex;
      }

      if (!strip || strip && !hex.match(SEQUENCE_REMOVAL_PATTERN)) {
        hexcode.push(hex);
      }
    }
  } catch (err) {
    _didIteratorError = true;
    _iteratorError = err;
  } finally {
    try {
      if (!_iteratorNormalCompletion && _iterator.return) {
        _iterator.return();
      }
    } finally {
      if (_didIteratorError) {
        throw _iteratorError;
      }
    }
  }

  return hexcode.join('-');
}