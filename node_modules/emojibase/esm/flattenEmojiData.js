import _extends from 'babel-runtime/helpers/extends';
import _objectWithoutProperties from 'babel-runtime/helpers/objectWithoutProperties';


export default function flattenEmojiData(data) {
  var emojis = [];

  data.forEach(function (emoji) {
    if (emoji.skins) {
      var skins = emoji.skins,
          restEmoji = _objectWithoutProperties(emoji, ['skins']);

      emojis.push(restEmoji);

      skins.forEach(function (skin) {
        var skinEmoji = _extends({}, skin);

        if (emoji.tags) {
          skinEmoji.tags = emoji.tags;
        }

        emojis.push(skinEmoji);
      });
    } else {
      emojis.push(emoji);
    }
  });

  return emojis;
} /**
   * @copyright   2017, Miles Johnson
   * @license     https://opensource.org/licenses/MIT
   * 
   */